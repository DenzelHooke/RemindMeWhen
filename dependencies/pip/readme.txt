chill-requirements.txt is a chilled version of pip freeze and can be used for Linux and Windows pip installs.

	chill-requirements.txt is just a backup file, copy this to your dir containing the venv and rename it to requirements.txt